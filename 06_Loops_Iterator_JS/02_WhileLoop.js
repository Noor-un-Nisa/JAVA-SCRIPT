// WHILE LOOP

let sum = 0

while (sum<20) {
    sum +=4
    console.log(`sum is ${sum}`);
}

// ARRAY AND WHILE LOOP

const Avengers = ['batman','ironman','thor','spiderman']
let length=0
while(length<Avengers.length){
    console.log(`${length+1} ${Avengers[length]}`);
    length++
}